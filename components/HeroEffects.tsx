/**
 * Слои hero. В варианте B — только тонкая виньетка:
 * цветные свечения и плёночное зерно убраны, они читались как «неон».
 */
export default function HeroEffects() {
  return <div className="vignette absolute inset-0" />;
}
